"math/rand" - генерація рандомного числа 
              number := rand.Intn(100)

"time" - робота з таймаутами, часом
         time.Sleep(500 * time.Millisecond)  
         time.Now().Hour(), ":", time.Now().Minute(), ":", time.Now().Second()           

"defer" - відкладений виклик функції в стеку. Аналог finally(), First In - Last Out

"& *" - вказівники, для позбавлення від копіювання великих обʼєктів/змінних
        Роблять змінні з посиланням на обʼєкт, зберігаються в heap

type Name struct{} - структури, основна одиниця побудови архітектури. Аналог класів

func (n *Name) SayHello() (string){} - метод структури, з ресивером-вказівником на неї

pakage/module - пакет на рівні директорії з файлами, модуль це весь проект на рівні go mod

[2]string{"Hello", "World"} - масив, фіксована довжина, містить будь-які дані, незмінний
[]string{"pr", "bg"} - слайс, динамічної довжини масив, має методи: append, copy, slice[:]
map[int]string{1: "ht", 2: "jk"} - хеш-мапа, аналог асоціативного масиву, для швидкого доступу

scanner := bufio.NewScanner(os.Stdin) - користувацький ввід тексту з консолі, підключення бібліотеки
text := scanner.Text() - вивід користувацького тексту з консолі і збереження у змінну
fields := scanner.Fields() - вивід слів окремо в слайсі